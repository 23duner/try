
---
[TOC]

2147483647
寻找一种 快速开始学习的方法
分析技巧
所有的算法名称都只是乱起的
while结束
用if else 把所有情况写清楚
# 算法
算法的时间复杂度取决于问题的规模 
算法分析的目的是分析算法的效率以求改进
## 树与图
### BFS-breadth
* 层次遍历
* 求广度优先生成树
### DFS-depth
* 前序遍历
* 所有边也都遍历过去了
* 非连通图 遍历的同时求解了所有连通分量
~~~
void DFSTraverse(Graph G, Status (*Visit)(int v) ) {
  VisitFunc = Visit;  
  int visited[Vexnum] ;
  for (v=0; v<G.Vexnum; ++v) 
       visited[v] = FALSE;     
       // 访问标志数组初始化
  for (v=0; v<G.Vexnum; ++v) 
     if (!visited[v])  
     DFS(G, v);
       // 对尚未访问的顶点调用DFS
}
~~~
* 求深度优先生成树  

### 连通性
### 有向无环图DAG
### 最小生成树prim算法
原来是贪心算法啊（战术后仰
G连通网
T最小生成树边的集合
U部分的顶点集合

### 最短路径（权值和
* 最小生成树

## 模式匹配算法KMP
* 子串的定位操作通常称作串的模式匹配
### 简单匹配
是真的简单粗暴，就是挨个比较，不行就主串的下一个再挨个和模式串比较     
### 定义
* 字典树=树+字符串
前缀后缀只是特殊子串而已 真前缀很像真子集 只是不包含自己
next值 默认next[1]=0，next[2]=1

主串的某一个子串等于模式串的某一个前缀。
要前一个字符的前缀表的数值呢，因为要找前面字符串的最长相同的前缀和后缀
移动到前一个字符的前缀表的数值
字典树+KMP=AC自动机
## 二分
没错我就是一个连二分都写不明白的废 ==分治== ==搜索==
有序遍历就考虑二分吗

>有一天阿东到图书馆借了 N 本书，出图书馆的时候，警报响了，于是保安把阿东拦下，要检查一下哪本书没有登记出借。阿东正准备把每一本书在报警器下过一下，以找出引发警报的书，但是保安露出不屑的眼神：你连二分查找都不会吗？于是保安把书分成两堆，让第一堆过一下报警器，报警器响；于是再把这堆书分成两堆…… 最终，检测了 logN 次之后，保安成功的找到了那本引起警报的书，露出了得意和嘲讽的笑容。于是阿东背着剩下的书走了。
>从此，图书馆丢了 N - 1 本书。   

1. 适用范围：
   1. 有序 排序算法复杂度甚至更高
   2. 数组 链表跳跃式访问效率太低
2. bbbbbbbug:  
   1. mid=left+(right-left)/2 避免int溢出
   2. while(区间非空) 
~~~
int binarySearch(int[] nums, int target) {
    int left = 0, right = ...;

    while(...) {
        int mid = left + (right - left) / 2;
        if (nums[mid] == target) {
            ...
        } else if (nums[mid] < target) {
            left = ...
        } else if (nums[mid] > target) {
            right = ...
        }
    }
    return ...;
}
~~~   
==旋转数组与二分==
二分之后 有一部分一定是有序的 刚好合适
## 双指针
工科思维之 这个玩意成立的原因是it works
弄懂的话只能大量整点题目做了
快慢指针 差值固定或者步长固定
对撞指针 当这些概念明确的时候 救命的铺路问题也可以解决掉诶好像 ==铺路====接雨水== 对撞指针能遍历到所有的情况 所以只要有维护和更新的意识就好
### 滑动窗口
某种连起来的双指针（？
这里的指针 在更大程度上应该称之为下标
可以做到遍历所有长度为某定值的区间
## 计算机思维之递归迭代遍历枚举查找
### 查找
如何计算平均查找长度(b+1)/2
### 递归recursion
调用自身
还没等脑子递归到栈底内存就先炸了
### 迭代
是循环

### 遍历
### 枚举
其实大部分时间都是在枚举和枚举优化 把枚举当成一个子部分呢
枚举的==范围==
这个好重要

### 关于迭代器for循环三种
~~~
vector<Point> points;
printf("通过迭代器访问\n");
    vector<Point>::iterator iter;
    for (iter = points.begin(); iter != points.end(); iter++) {

    }
~~~
为什么你会忘记while也是循环  while循环是范围循环
for循环是条件循环 但是auto for 也是基于范围的循环吧
## 排序
无所谓我会`sort(arr.begin(),arr.end())`
学完排序就去翻新生赛玩
算法思想的本身是独立于编程语言的
* 稳定与不稳定的排序算法 
  通俗地讲就是能保证排序前两个相等的数其在序列的前后位置顺序和排序后它们两个的前后位置顺序相同。*在简单形式化一下*，如果Ai = Aj，Ai原来在位置前，排序后Ai还是要在Aj位置前。  
  
算法就像轮子
多学一个算法就多一个脑子
### 冒泡排序
~~~
 所有的数组 起手都应该是一句
n=len(nums)-1
 py的range不算最后一位的吧
for i in range(n-1):
  对数组未排序区间 [0, n - i - 1] 的元素执行「冒泡」
    for j in range(n-i-1)：
        if():
            nums[j], nums[j + 1] = nums[j + 1], nums[j]
            
~~~
效率优化之flag 标志的储存
### 插入排序
性质和冒泡很像
### 堆排序
* 筛选：从堆顶到叶子的调整过程
### 快排
交换类的排序包含冒泡和快排
快排是分治思想？有基准划分即可称为分治
为什么要把所有排序都学掉aaa 快排比较重要 确信
~~~
/* 元素交换 在一个数组中交换下标为i和j的两个元素 明确你输入的是什么东西*/
void swap(vector<int> &nums, int i, int j) {
    int tmp = nums[i];
    nums[i] = nums[j];
    nums[j] = tmp;
}

/* 哨兵划分 
然鹅这里实际上是个函数咧 所以在代码块中使用的时候记得把left改成0 right改成n*/
int partition(vector<int> &nums, int left, int right) {
    // 以 nums[left] 作为基准数
    int i = left, j = right;
    //看清楚这里是逗号
    while (i < j) {
        while (i < j && nums[j] >= nums[left])
            j--; // 从右向左找首个小于基准数的元素
        while (i < j && nums[i] <= nums[left])
            i++;          // 从左向右找首个大于基准数的元素
        swap(nums, i, j); // 交换这两个元素
    }
    //到这一步的时候i=j所以这里用哪个其实不重要
    swap(nums, i, left); // 将基准数交换至两子数组的分界线
    return i;            // 返回基准数的索引
}
~~~
### 桶排序
### 选择排序
不用一趟一趟跑了
## 哈希
*最致命的一集`hash[nums[i]]++; `里面好歹得用到nums啊
*有负数怎么用哈希啊啊啊 nums[i]可以为负数的说救命
细节与优化
*与众数有关系的摩尔投票 其实我觉得有点像动态规划  
## 数学
### 公式（袖珍计算器
$\sqrt{x}=x^{1 / 2}=\left(e^{\ln x}\right)^{1 / 2}=e^{\frac{1}{2} \ln x}$
指数`exp()`对数函数`log()`全部返回浮点值
### 数论
gcd()最大公因数

---
四月才打蓝桥杯 总得水个奖回来
蓝桥杯备赛
比赛位列前60的同学可以在本学期选修董开坤老师开办的“面向问题求解的程序设计”创新创业课程，课上主要讲解算法竞赛中的热门算法和数据结构，以及众多的解题方式和技巧，同时会有校ACM队的学长们授课，不仅可以丰富专业知识，也可以为蓝桥杯这类竞赛提供一定的知识储备和题量储备。
一水（会点c++_算法的犬犬） 2023/10/15 0:48:48
想跟姐妹们分享一下之前准备蓝桥杯的心路历程~做题是在蓝桥云课（https://www.lanqiao.cn/problems/?first_category_id=1&sort=students_count&second_category_id=3)，刷蓝桥杯真题，从最近的一年开始把这几年刷一遍，重在稳定拿分。因为蓝桥杯有部分分，而且部分分给的很够，有40%的，打暴力很多也有60%的分数。最后现场失眠了状态不好，除了前面小题争取拿满，后面的大题没有想到正解，全部打了60%的暴力！结果还踩线一等倒数哈哈~总结一下就是非算法题（前面的题，学完c语言就能做）尽量拿满，然后学一些基础算法（可以先学dfs和bfs，打了两年都有出专门考dfs的题，然后是二分、贪心、最短路、简单dp这些），到这里基本就有省奖，省二/省三；时间充裕的话可以把难一点的算法也学了，会基本应用，蓝桥杯很喜欢考模板题（去年就考了一道后缀自动机模板题，相当于默写代码hh），就可以省一了~

一水（会点c++_算法的犬犬） 2023/10/15 0:54:26
然后@笨猫猫 是大一c语言基础打得很扎实，相较于我没怎么做题，“临场前bfs和dfs都不会，可以说一点算法都不会，纯摸鱼摸了个省三”这是她原话哈哈，所以姐妹们可以大胆冲冲！



我:
就是说  如果这学期末或者下学期我可能可以选到好像是zzw当助教的数据结构与算法课，我要不要选
我:
（）

:
（）
:
他咋去当助教了
:
那门课考满分了？
我:
他是高中信息竞赛佬
我:
（）
我:
之前的政策是这样的
我:
（
我:
《校acm队》
我:
真无语啊
我:
（）
:
()
:
他是那个队的？
我:
「焉识：他是那个队的？」

确实
:
（）
:
难绷
我:
还是队里大二排第二的负责人好像
:
冇所谓啊
我:
我就发发癫
:
你是为了你自己
:
又不是为了他
:
就当不认识呗
我:
遇到前男友：恶灵退散恶灵退散
:
我们英辩社的两个部长还是前任呢
我:
6
:
非常之尴尬
:
把我夹在中间
:
非常之尴尬
:
都是我们专业的
:
所以
:
冇所谓
:
你那又不是小范围的
:
大范围活动
:
更是见了当没见
我:
没逝，这个课上应该他认识的人多（
:
主打一个装死
我:
他尴尬
我:
我就不尴尬
:
没逝